# Repository workflow

- After making any code changes, commit them and push the commit to the configured remote before finishing the task.
- Include only changes relevant to the current task in each commit.
